/**
 * OSMify
 *
 * @param {(Feature|FeatureCollection)} json GeoJSON Feature or FeatureCollection
 * @returns {(Feature|FeatureCollection)}
 */
function osmify (json) {
  return json
}

module.exports = osmify
